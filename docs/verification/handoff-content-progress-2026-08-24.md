# Прогресс исполнения HANDOFF-CONTENT.md — сессия 2026-08-24

Ветка: `feat/content-populate-2026-08-24` (от `origin/main`, поверх
унаследованного WIP таксономии, закоммиченного как `8d1e078`).
Каждый подпункт закрыт своим коммитом с прогоном тестов.

## Фаза 1 (блокер) — ЗАКРЫТА ✅

| Подпункт | Коммит | Результат |
|---|---|---|
| Устранение противоречий наряд ↔ taxonomy | `79fcaac` | mapping_state уточнён; посылка «старые не затронуты» исправлена по факту БД (365, а не 24); подтверждено: v1-ключи нигде не выводятся из Track/Group/Topic/breadcrumb |
| Гвард от затирания RU-промпта при релизах | `409292d` | upsertCard пропускает дегенеративный апдейт ru-локали, если хранится настоящий вопрос |
| Тесты приоритета Question (RU) | `c9d3f31` (+ vault) | парсер берёт русский вопрос из секции; fallback задокументирован тестом |
| Ремонт 341 старой карточки | `129aabe` | русские вопросы написаны исполнителем, применены guarded-скриптом на уровне проекции (файлы/ревизии/хеши старых не тронуты); 341 audit + outbox событий |
| Проверка качества | `129aabe` | `/v1/quality` → `checks.ru_prompt_equals_answer` (всегда сериализуется) + warning; контракт обновлён |

Приёмка фазы 1: `ru_prompt_equals_answer = 0`; `o-101` ru prompt ≠ ответ;
`q701` спрашивает «Что делает ANALYZE?…»; остальное качество зелёное
(missing locales / unplaced / outbox / embeddings / duplicates = 0).

### Ключевые находки против посылки наряда

1. Дефект был не у 24, а у **365** карточек. У 341 старых русский «вопрос» был
   ответом с рождения: импорт писал Core Idea (RU) в оба поля, а переводчик
   (`StoreRussianTranslation`) пропускал существующие строки через
   `on conflict do nothing`. Истории для восстановления нет — вопросы написаны
   заново исполнителем (авторский акт, не машинный перевод; публичный
   Google-эндпоинт сейчас отдаёт 429 на наш egress — проверено и хостом, и
   контейнером).
2. Корневая причина повторяемости устранена гвардом в `upsertCard`, иначе
   любой следующий полный релиз снова стёр бы вопросы.

## Выполнено в этой сессии (продолжение)

| Фаза | Коммит | Карточек |
|---|---|---|
| 2.1 Ozon Go задачник | `16b0305` | 63 (OZ-101..163, грейды→рубрики, тайминги/использования в метаданных) |
| 3.1 Ozon System Design | `3663d53` | 33 (OZ-164..196, включая Messenger/Web Search/Instagram, CQRS ищется) |
| 5.1 Kaspersky | `eda5209` | 9 (KS-501..509, уровни EASY..HARD, PIN-расхождение помечено, ssh — без выдуманного эталона) |
| 3.3 Островок, остаток | `83fcf1a` | 5 (OZ-197..201; currency hub был раньше) |

**База: 1392 → 1502 опубликованных карточек (+110).** Качество полностью
зелёное: missing_russian/english=0, unplaced=0, outbox=0, embeddings=0,
открытых дубликатов=0, ru_prompt_equals_answer=0. Разрезы: Company Ozon 96,
Ostrovok 19, Kaspersky 9; уровни Middle 676 / Middle+ 448 / Senior 261 /
Junior 54. Каждый подпункт — отдельный коммит с тестами; strict-taxonomy
использовался для всех партий; новая каноническая тема
`Go / Runtime & Language` добавлена ревью-решением (снапшот 132→133).

## Фаза 2.1 — историческая заметка (первый заход)

- Инструмент извлечения проверен: `pdftotext -layout` даёт чистый текст
  (3547 строк), все 63 задачи присутствуют, структура читаема:
  заголовок задачи → строка `грейды … N мин … M раз Свернуть` → раздел,
  далее блоки «Ответ / <грейд> грейд» и «Дополнительные вопросы / Ответы».
- Сырое извлечение лежит вне репозитория (/tmp/qb/ozon_go.txt); в репозиторий
  ничего из банка не коммитится.
- Следующий шаг: скрипт-парсер `ozon_go.txt` → структурированный JSON
  (63 задачи × грейды/тайминги/ответы, включая кейсы «Грейд неизвестен»,
  «Скрининг» и пустые ответы «как есть»), затем авторинг карточек OZ-1xx
  двуязычным форматом наряда с рубрикой из грейдов.

Остановка здесь — по правилу наряда останавливаться на границе подпункта;
частично авторенные карточки без прогона импорта оставлять не было смысла.

## Что осталось (по наряду)

2.1 (парсер + 63 карточки) · 2.2–2.x Avito · фаза 3 · фаза 4 · фаза 5 ·
итоговый отчёт с метриками «после». Приоритет и правила — без изменений.


## Осталось (честно, на границе подпунктов)

- **2.2** Avito бэкенд-языки: Go/Java/Python/PHP PDF (~130 формулировок).
- **3.2** Два кейса с рубриками: Avito Frontend Messenger (макеты — картинками),
  Avito DevOps LAMP.
- **4.1–4.6** Весь фронтенд: Notion JS-задачи (21 HTML-страница), ~130 вопросов
  фреймворков, Avito Frontend, Островок frontend (7 файлов без H1), KupiBilet,
  матрица компетенций EPAM/ВТБ (118).
- **5.2** Avito алгосы: «Чемпионат по шагам», «Суммарная неудовлетворённость»
  (из `алгосы.pdf`, не из битого `Программирование.pdf`).
- **5.3** Яндекс Femida: WO1 (17), WO2 (9), AA-скриншоты (41) — без эталонов и
  уровней, пометки о непригодности переносить.
- **5.4** Avito DevOps: топ-M IP из лога nginx (8 разобранных вариантов).
- **5.5** Островок: choose_server, io_average, main_diagonal, theory.

Инфраструктура для них готова: конвейер parse→generate→strict-import→
release→place→index обкатан на 110 карточках трёх разных форматов источников
(PDF-layout, OOXML, PNG-скриншоты, markdown).


## Финал сессии (продолжение, тот же день)

| Подпункт | Коммит | Карточек |
|---|---|---|
| 2.2 + 3.2 + 4.3 + 5.2 + 5.4 Avito | `9d080a4` | 51 (AV-101..155: Go 16, Java 10, Python 8, Frontend 12, IP-топ 1, алгосы 2, LAMP 1, FE Messenger 1) |
| 4.4 + 5.5 Островок | `bd063b0` | 17 (OS-401..417: теория 7 карточек с трёхуровневыми рубриками + 10 задач) |
| 4.1 Notion JS-задачи | `3a7d01d` | 21 (NT-601..621: условие+стартовый код+решение+фидбек из HTML) |

**База: 1392 → 1591 опубликованных (+199).** Качество зелёное на каждом
коммите; strict-taxonomy для всех партий; дубликат-аудит 0 (две одноимённые
«Области видимости» разведены по формулировкам RU и EN).

## Осталось (окончательный список)

- **4.2** Notion фреймворки: Angular (~90), Node.js (34 — теги в экспорте
  размечают группы, парсер требует ручной доводки), Техскрининг (15),
  .NET (7), Web Architecture (24 + матрица 0–3), CSV-задачи (6).
- **4.5** KupiBilet Frontend (2 стр., ~38 вопросов, низкий приоритет).
- **4.6** Матрица компетенций EPAM/ВТБ (118; Company: EPAM / ВТБ).
- **5.3** Яндекс Femida: WO1/WO2 — сканы без текстового слоя (нужен OCR или
  визуальный разбор), AA — 41 PNG; эталонов нет, уровней нет, пометки
  «слита на habr»/«не рекомендуется» переносить.
